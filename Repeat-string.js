//Write a function that accepts a non-negative integer n and a string s as parameters, and returns a string of s repeated exactly n times.
function repeatString(str, n){
    return str.repeat(n);
}

console.log(repeatString("taha", 5))